<%= form_with(model: area, local: true) do |form| %>
  <% if area.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(area.errors.count, "error") %> prohibited this area from being saved:</h2>

      <ul>
        <% area.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
<div class="form-row">
  <div class="form-group col-md-3">
    <%= form.label :area_name, "Nombre del Área" %>
    <%= form.text_field :area_name, class:"form-control" %>
  </div>

  <div class="form-group col-md-3">
    <%= form.label :area_code, "Código" %>
    <%= form.text_field :area_code, class:"form-control" %>
  </div>

</div>
  
  <div class="actions">
    <%= form.submit "Registrar" %>
  </div>
<% end %>
