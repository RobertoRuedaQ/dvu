<div class="row">
  <div class="col-md-3">
    <h1>Sedes</h1>
  </div>
  <div class="col-md-3"></div>
  <div class="col-md-3"></div>
  <div class="col-md-3">
    <%= link_to 'Registrar Nueva Sede', new_campus_path, class: "btn btn-outline-success" %>
  </div>
</div>

<table class="table table-hover table-responsive-sm">
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Código</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @campus.each do |campus| %>
      <tr>
        <td><%= campus.campus_name %></td>
        <td><%= campus.campus_code %></td>
        <td><%= link_to campus do %>
          <i class="fas fa-eye"></i>
          <%end %>
        </td>
        
        <td><%= link_to edit_campus_path(campus) do %>
            <i class="far fa-edit"></i>
          <%end %>
        </td>

        <td><%= link_to  campus, method: :delete, data: { confirm: 'Are you sure?' } do %>
            <i class="far fa-trash-alt"></i>
          <%end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>