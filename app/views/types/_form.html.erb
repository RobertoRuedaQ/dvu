<%= form_with(model: type, local: true) do |form| %>
  <% if type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(type.errors.count, "error") %> prohibited this type from being saved:</h2>

      <ul>
        <% type.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
<div class="form-row">
  <div class="form-group col-md-3">
    <%= form.label :type_name, "Tipo de actividad" %>
    <%= form.text_field :type_name, class: "form-control" %>
  </div>

  <div class="form-group col-md-3">
    <%= form.label :type_code, "Código de actividad" %>
    <%= form.text_field :type_code, class:"form-control" %>
  </div>
</div>
  

  <div class="actions">
    <%= form.submit "Registrar", class: "btn btn-outline-dark" %>
  </div>
<% end %>
