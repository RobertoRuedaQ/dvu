<div class="row">
  <div class="col-md-3">
    <h1>Tipos de actividades</h1>
  </div>
  <div class="col-md-3"></div>
  <div class="col-md-3"></div>
  <div class="col-md-3">
    <%= link_to 'Registrar Nuevo Tipo de Actividad', new_type_path, class: "btn btn-outline-success" %>
  </div>
</div>

<table class="table table-hover table-responsive-sm">
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Código</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @types.each do |type| %>
      <tr>
        <td><%= type.type_name %></td>
        <td><%= type.type_code %></td>
        <td><%= link_to type do %>
          <i class="fas fa-eye"></i>
          <%end %>
        </td>
        
        <td><%= link_to edit_type_path(type) do %>
            <i class="far fa-edit"></i>
          <%end %>
        </td>

        <td><%= link_to  type, method: :delete, data: { confirm: 'Are you sure?' } do %>
            <i class="far fa-trash-alt"></i>
          <%end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>